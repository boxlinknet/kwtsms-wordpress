vendor/
node_modules/
*.log
.DS_Store
auth-state.json

# PHPUnit / Composer generated files
.phpunit.result.cache
composer.lock

# Screenshots and docs live in the repo root docs/ folder, not inside the plugin
docs/
